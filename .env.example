# Cloudflare Pages 部署設定。複製成 .env 後填入；.env 不進版控。
#
# 本機若已跑過 `wrangler login`，token 可以留空，deploy.sh 會改用該 OAuth session。
# 需要 token 時（例如 CI）：https://dash.cloudflare.com/profile/api-tokens
#   Create Token -> "Cloudflare Pages" 範本 (Edit) -> Continue -> Create Token
CLOUDFLARE_API_TOKEN=
# 執行 `npx wrangler whoami` 可取得。Account ID 不是機密。
CLOUDFLARE_ACCOUNT_ID=
# Pages 專案名稱 -> https://<name>.pages.dev
CF_PAGES_PROJECT=data-analysis-with-ai-agent
# 要部署的目錄。本專案的靜態站台在 site/，不是根目錄。
CF_PAGES_DIR=site
CF_PAGES_BRANCH=main
